import { IsAllowDirective } from './is-allow.directive';

describe('IsAllowDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAllowDirective();
    expect(directive).toBeTruthy();
  });
});
